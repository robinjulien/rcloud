import { SortfoldersfilesPipe } from './sortfoldersfiles.pipe';

describe('SortfoldersfilesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortfoldersfilesPipe();
    expect(pipe).toBeTruthy();
  });
});
