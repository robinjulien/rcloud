import { HumanreadablesizePipe } from './humanreadablesize.pipe';

describe('HumanreadablesizePipe', () => {
  it('create an instance', () => {
    const pipe = new HumanreadablesizePipe();
    expect(pipe).toBeTruthy();
  });
});
